/* Create struct for 32_bit 16_bit and 3 byte. What will be the size of above struct */

#include<stdio.h>

#define MAX(a,b) (a>b ? a:b)

int main(int argc, char *argv[])
{
  printf("Max in 8 and 4 is: %d \n", MAX(8,4));

  return 0;
}
